createdb.sql -- creates all the tables in "pizza" database (assumes database has already been created)

DataGenerator.class -- file generated when DataGenerator.java is compiled

DataGenerator.java -- java program to generate data for users, recipes, and ordrs.  Generated data is exported to a text file with csv format.  

data_inventory.txt, data_orders.txt, data_recipes.txt, data_users.txt -- text files with csv format that contains data for respective tables. 

loadInventory.sql, loadMap.sql, loadRecipes.sql, loadUsers.sql -- files that contain the SQL commands used to load data from the csv formatted data files into the respective tables in the database.  loadMap.sql contains only a SQL INSERT command.  

takeOrders.sql -- the takeOrders server side function.  
