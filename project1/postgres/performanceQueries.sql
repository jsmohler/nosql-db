EXPLAIN SELECT * FROM orders WHERE rec_name = '1';
EXPLAIN SELECT * FROM orders WHERE rec_name = '2';
EXPLAIN SELECT * FROM orders WHERE rec_name = '3';
EXPLAIN SELECT * FROM orders WHERE rec_name = '4';
EXPLAIN SELECT * FROM orders WHERE rec_name = '5';
EXPLAIN SELECT * FROM orders WHERE rec_name = '6';
EXPLAIN SELECT * FROM orders WHERE rec_name = '7';
EXPLAIN SELECT * FROM orders WHERE rec_name = '8';
EXPLAIN SELECT * FROM orders WHERE rec_name = '9';
EXPLAIN SELECT * FROM orders WHERE rec_name = '10';
EXPLAIN SELECT * FROM orders WHERE rec_name = '11';
EXPLAIN SELECT * FROM orders WHERE rec_name = '12';
EXPLAIN SELECT * FROM orders WHERE rec_name = '13';
EXPLAIN SELECT * FROM orders WHERE rec_name = '14';
EXPLAIN SELECT * FROM orders WHERE rec_name = '15';
EXPLAIN SELECT * FROM orders WHERE rec_name = '16';
EXPLAIN SELECT * FROM orders WHERE rec_name = '17';
EXPLAIN SELECT * FROM orders WHERE rec_name = '18';
EXPLAIN SELECT * FROM orders WHERE rec_name = '19';
EXPLAIN SELECT * FROM orders WHERE rec_name = '20';
EXPLAIN SELECT * FROM orders WHERE rec_name = '21';
EXPLAIN SELECT * FROM orders WHERE rec_name = '22';
EXPLAIN SELECT * FROM orders WHERE rec_name = '23';
EXPLAIN SELECT * FROM orders WHERE rec_name = '24';
EXPLAIN SELECT * FROM orders WHERE rec_name = '25';
